## 1. Change Gate

- [x] 1.1 Complete and strictly validate the proposal, design, new dashboard capability spec, and modified Core schema, Store, Explorer, and multi-device capability specs before modifying production or test source.
- [x] 1.2 Obtain lightweight independent architecture/API, correctness/testing, and security/performance/documentation reviews of the artifacts and resolve every actionable finding.

## 2. Shared Schema and Raw Performance Traversal

- [x] 2.1 Move the exact ordered 16-key metric inventory, group, and numeric/categorical/unavailable-only kind from SDK internals to Core `NearWireInternal` SPI; update SDK and Viewer consumers without public API, JSON, validation, collection, or unknown-key behavior changes, and leave no duplicate raw-string inventory.
- [x] 2.2 Add immutable performance source, frozen receipt, last-examined continuation, bounded/oversized carrier, page, Store/live gap wrappers with generic pagination plus saturating applicable count/`hasMoreApplicableGaps`, normalized gap, and error values with content-free reflection; enforce 65,536-byte rows, 4,194,304 copied content, 512-byte Event carriers, 256-byte gap carriers, 4,096/512-byte wrappers, 4,460,544-byte Event pages, 4,493,312-byte live slices, and 8,704-byte gap pages.
- [x] 2.3 Implement ascending device-timeline candidate scanning with residual type filtering, exact matching/nonmatching continuation progress, pre-copy SQLite length classification, accepted no-sort plans, 512-emitted/4,096-examined/5,000,000-VM/injected-50-ms bounds, deterministic empty turns, and terminal no-progress work failure.
- [x] 2.4 Integrate Event/gap traversal into the query arbiter and Store explorer gateway with exact generation tokens, one finite lease, continuation-safe cancellation, replacement rejection, complete frozen metadata classification under 2,000,000-VM/injected-250-ms limits, distinct generic versus applicable overflow, closed kind/reason and applicability/direction mapping, conservative unknown/budget failure, no variable strings, and joined cleanup.
- [x] 2.5 Expose an exact-connection live-first freeze that drains bounded ingress, returns at most one 4,493,312-byte slice including 128 gaps, saturating applicable-loss total/`hasMoreApplicableGaps`, generation, and anchor, then freezes Store Event/gap uppers and reconciles every capture/commit permutation by canonical journal tuple without adding persistence or migration.

## 3. Typed Projection and Aggregation

- [x] 3.1 Implement Core V1 decode by consuming the shared 16-key SPI inventory with whole-snapshot invalidation for malformed/schema/size/duplicate-known-unavailable/present-plus-unavailable input; preserve measured zero, categorical unknown, every unavailable reason, not-collected, stale, and unknown raw-only keys distinctly.
- [x] 3.2 Implement ten metric-specific accumulators and representatives, 512 buckets, bounded categorical change counts, 128 gap/128 invalid details, discontinuity flags, 12,288 mark/64-accessibility derivation, 8-MiB result, exact per-object charging, global 16-MiB shared ledger, and exact 25,805,312-byte peak ownership.
- [x] 3.3 Implement checked inclusive range bounds and bucket formula, live-first/current and ended/interrupted/empty anchors, Viewer receive ordering, five-minute default, complete canonical cache comparator, canonical journal comparator, exact-hit/LRU touch, deterministic fifth insertion, source/device clearing, and no raw-sample array.
- [x] 3.4 Implement closed Store/live gap normalization, irrelevant-versus-applicable/uncertain handling, generic versus applicable overflow, monotonic wall-envelope placement, interval-less/unknown/`hasMoreApplicableGaps`/combined-overflow Unplaced suppression, per-metric holes, all-metric invalid breaks, detail overflow without reconnection, mixed-bucket availability counts/precedence, latest-Event card state after freshness precedence, invalid-header three-second horizon, and checked 180-second lookback.
- [x] 3.5 Implement finite page/decode turns, one running scan plus dirty successor, latest-only 10-Hz delivery, current source/Event/absolute-deadline/revision receipts with claim/apply uptime validation and one future-only wake, historical same-recording frozen-upper evaluation with no wake, bounded paused-expiry dirty state, current/historical/Pause scope replacement, historical-unavailable/current-live-only/mid-scan failure/recovery states, and no partial-complete publication.

## 4. Presentation and Raw Traceability

- [x] 4.1 Add one MainActor dashboard model for exact single-device scope, range, current cards with distinct current deadline versus historical frozen-anchor receipts, chart groups, gaps, availability, progress/errors, crosshair, and content-free diagnostics.
- [x] 4.2 Add a lifecycle-owned dashboard controller that binds Store/live projection, validates freshness at delivery claim/apply, handles deadline-versus-Pause/Resume/late-result ordering and paused desired range, immediately joins/clears on source/device/runtime replacement, owns deadline/delivery/cache cleanup, and releases every charged value before the combined receipt.
- [x] 4.3 Implement metric-specific journal-key Open Source Event resolution with durable-then-live locator lookup, exact source validation/reload, live-to-durable continuity, stale/deleted/evicted guidance, and no content-model sharing.
- [x] 4.4 Add one analysis-mode coordinator that releases and joins the current Event or Performance traversal before successor submission/reveal, then integrate Events/Performance mode and one-device guidance without duplicating any protocol, Store, live, Explorer, or composer owner.

## 5. Native macOS Performance UI

- [x] 5.1 Build scalable current cards plus fixed 16-key availability section for FPS, CPU, memory, battery, thermal, low-power mode, queue depth, drops, byte rates, and unavailable-only GPU/power/Celsius with units, freshness, and closed state text.
- [x] 5.2 Build six system Swift Charts views with average/min-max disclosure, per-metric discontinuities, Unplaced-gap disconnected rendering, and exact 512-bucket/12,288-total-mark bounds.
- [x] 5.3 Add one synchronized keyboard/pointer crosshair, aggregate tooltip, representative raw-Event action, fixed range controls, loading/empty/error/single-device states, and no derived-data export.
- [x] 5.4 Add deterministic English accessibility labels, non-color states, at most 64 accessible buckets per chart, scalable layout, redacted reflection, and no copy/cut/drag/share/clipboard/persistence surface for received metrics.

## 6. Tests, Documentation, and Evidence

- [x] 6.1 Add Store tests for exact scope/order/plan/gap freeze and 4,095/4,096/4,097 nonmatches, 511/512/513 matches, empty continuations, before/after-match stops, equal ties, 65,536/+1 rows, Event/gap page and live-slice accounting, complete kind/applicability mapping, paired identical-retained 129 irrelevant-only versus hidden-applicable tails, classification budget failure, normalized string exclusion, injected 49/50/exceeded ms and VM bounds, cancellation/replacement/lease release, schema 2, and no projection artifact.
- [x] 6.2 Add Core/SDK/Viewer tests proving one identical ordered 16-key/group/kind SPI inventory and no duplicate raw-string enum; add decode tests for real zero, categorical unknown, every reason, absence, unknown raw-only keys, identical/conflicting duplicate unavailable, present-plus-unavailable, latest invalid/missing, malformed/schema/JSON, 65,536/+1 bytes, and no oversized content copy.
- [x] 6.3 Add aggregation tests for 0/1/512/513/100,000 samples, ten metrics with disjoint contributors, exact edges and canonical equal-tie comparators, min/max/average/count, mixed states, finite overflow, metric representative ties/live-to-durable locator, categorical/gap/invalid storms, every deterministic per-object formula, 8/16/25,805,312-byte bounds, fifth LRU insertion, and no raw array.
- [x] 6.4 Add exact current/ended/interrupted/empty range and bucket geometry, underflow/overflow, card lookback/range independence, no-snapshot/invalid-header/fresh-invalid/fresh-unavailable/stale precedence, current freshness equality/deadline, historical anchors below/equal/above current uptime and simulated uptime reset with zero wake, 100-ms-through-60-s intervals, wall regression/unplaced gaps, bucket-edge/in-bucket gaps, metric holes, 127/128/129 Store/live/combined gap receipts, paired irrelevant/hidden-applicable tails, unknown/interval-less gaps, overflow suppression, live-only coverage, and no interpolation tests.
- [x] 6.5 Add barrier-controlled live-freeze/Store-upper/commit/reconciliation permutations and unavailable-before/mid/final-page recovery; cross current deadline before/after scan completion, MainActor claim/apply, Pause/Resume, current/historical switching, uptime reset, and source/runtime replacement; test running-plus-dirty 100,000 refreshes, one current wake/zero historical wake, no stale-to-fresh reversal or past-deadline loop, Pause/source/device/range/mode transitions, Store/runtime replacement, constant cache/bytes, and zero late/predecessor state.
- [x] 6.6 Add UI/integration tests for one-device guidance, traversal-ordered Events/Performance switching, fixed inventory cards/units, bounded marks/crosshair/tooltips, metric-specific reveal and stale/deleted/live-durable resolution, keyboard/accessibility caps, privacy, and complete lifecycle clearing.
- [x] 6.7 Add a deterministic 100,000-sample benchmark with alternating categorical/gap/invalid input and exact candidate/VM/clock/bucket/cache/diagnostic/mark/accessibility/byte/cleanup counts; record host timing/heap only as diagnostic context and add no shell harness.
- [x] 6.8 Add English Viewer performance documentation covering raw-event authority, ranges, cards, units, receive-time semantics, bucketing, crosshair, gaps, unavailable/stale/invalid states, raw reveal, bounds, privacy, cleanup, exclusions, and signing deferral.
- [x] 6.9 Build the unsigned Viewer, run the complete Viewer and affected root package suites, lint Swift, inspect package/project/plists/privacy, strictly validate OpenSpec, and save exact commands/results under this change's evidence directory.

## 7. Independent Completion Review

- [x] 7.1 Obtain independent architecture/API, correctness/testing, and security/performance/documentation implementation reviews and save every report.
- [x] 7.2 Fix every actionable finding, rerun affected and complete validation, and repeat all three review dimensions until a fresh round reports zero unresolved findings.
- [x] 7.3 Complete a requirement-to-evidence audit, archive `viewer-performance-dashboard`, verify canonical specs and archived evidence, and commit the completed change before starting `demo-distribution-e2e`.
