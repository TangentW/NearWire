## 1. Planning and Pre-Apply Gate

- [x] 1.1 Complete and strictly validate the proposal, design, new active-pump capability, all modified capability deltas, and this task plan.
- [x] 1.2 Run independent architecture/API, correctness/testing, and security/performance/documentation reviews of the planning artifacts; record every finding, remediate, and repeat fresh rounds until all three dimensions report zero unresolved findings.

## 2. Reserved Secure Mailbox Admission

- [x] 2.1 Add overflow-safe nonnegative count/byte reservations plus a constant-size capacity snapshot, progress generation, and non-retaining known-size predicate to synchronous secure-mailbox admission while preserving zero-reservation behavior, FIFO order, linearization, terminal cleanup, and no partial retention.
- [x] 2.2 Add deterministic boundary, arithmetic, concurrency, FIFO, completion-release, predicate/admission race, progress-generation, insufficient-known-size, and terminal-race tests proving Event admissions preserve exact Control capacity.

## 3. NearWire Queue and Wake Integration

- [x] 3.1 Add one internal gate-authorized tokenized outbound-work callback registration that atomically returns level-triggered owner availability plus its initial fair-candidate/deadline snapshot; make shutdown-first reject assignment and assignment-first signal persistent shutdown, route all hints through a synchronous lock ingress that coalesces before Task creation, retains at most one weak-routing Task plus one dirty successor, and removes only the exact token.
- [x] 3.2 Add bounded owner-aware queue scheduling observation and active offering with a captured maximum accepted-Event allowance distinct from positive service/byte bounds, exact instance-local origin clock, due-work continuation result, per-expiry/route-drop/accepted-candidate gate authorization, nonmutating fair planning, candidate-local sequence planning, exact envelope/TTL encoding, reserved mailbox admission, split committed-prefix/live-result counter semantics, and constant-size blocked-candidate requirements without external reservation.
- [x] 3.3 Add lock-controlled deterministic tests for shutdown before/during/after registration and empty/zero-rate refresh, prebuffered/installation-snapshot/newly signalled/cleared work, terminal-versus-registration, stale registration, notification storms before Task creation, captured-token/service/byte prefix matrices, multi-quantum due expiry, per-expiry and route-drop terminal gates, exact actor-local clock/TTL bytes, encoding/clock/sequence failures, mailbox backpressure, terminal-before/after-candidate commit and result delivery, telemetry, retry identity, and unchanged public API behavior.

## 4. Active Pump Ownership and Policy

- [x] 4.1 Add validated `SDKActiveEventPumpLimits`, exact bound-session-clock and barrier-capable operation dependencies, the shared cross-actor operation gate, closed active/observer error codes, run/observer cancellation tokens and gates, one side-effect-free-until-run starter, one returned lifetime handle, and a one-shot cancellation-safe termination observer that retains neither handle nor relay.
- [x] 4.2 Extend the permanent session core through `bindingActiveOwner`, negotiating-policy, and active phases without changing callback, ingress, decoder, codec, route, relay, or terminal ownership; at runner claim start one continuous binding-plus-policy deadline and use an ingress-lock `running`/`nonterminalPaused`/`stopped` handshake with terminal/overflow bypass, exact scheduled-latch parking/unparking, no paused self-reschedule, and no post-terminal resume; add irreversible unclaimed/pull/runner policy-consumer ownership with exact precedence.
- [x] 4.3 Implement initial Viewer-offer/App-minimum acceptance, required capability checks, zero-rate pause, policy deadline, and complete ordered dynamic policy transactions that wait for captured-time old-policy work, prepare both bucket copies at a fresh commit clock before mailbox admission, and install them nonthrowingly at the exact acceptance boundary.
- [x] 4.4 Add barrier-controlled deterministic tests for idle construction, terminal/pre-registration/late-run/explicit/final-handle cancellation, activation ownership transfer, non-owning observer terminal-versus-cancellation ordering, second run, pre-pause callback delivery, parked repeated input, terminal/overflow bypass, bind resume, stop versus pause/resume, deadline-before-registration/registration-before-deadline/registration-success-with-no-offer/terminal-versus-deadline/stale-attachment-deadline/activation-versus-deadline, every pull/runner ownership precedence, buffered/coalesced offers, missing capabilities, invalid policy direction/order, zero/conservative rates, dynamic offers during drain and publication, fresh commit-clock failure, multiple transaction order/backpressure/overflow, and ownership continuity without sleeps.

## 5. Bounded Uplink Pump

- [x] 5.1 Implement one uplink bucket whose refreshed captured-time copy yields a whole-token acceptance allowance, expose the Core internal nonthrowing prevalidated token commit, and compose drain token/Task, coalesced work flag, exact token-or-origin-TTL decision wake, owner-aware bounded service/byte continuation, immediate due-maintenance continuation, transport block, cheap capacity retry, fair-candidate probe, lost-wake closure, and stale-result rejection without polling.
- [x] 5.2 Preserve one-frame-per-Event mailbox FIFO, admit no more live Events than the captured allowance, return contiguous planned App sequence only for accepted bytes, atomically install prevalidated-consumed bucket plus counter only for a live matching result, reserve two maximum Control sends, and keep rejected/not-attempted work unchanged with exact statistics.
- [x] 5.3 Add barrier-controlled deterministic zero/fractional/one/burst-token cross-products against smaller/equal/larger service, byte, depth, and mailbox bounds; include expiry/route drops before candidates, high-depth fairness, pause/resume, actor-local clock reversal, owner shutdown in policy/empty/zero-rate/active states, queue-signal storm, Control reservation, completion-before-block-result, repeated insufficient completion without re-encoding, dynamic-policy deferral, terminal between committed prefix and result, exact token snapshot/wake/telemetry, and byte-exact route/sequence/TTL tests without sleeps.

## 6. Bounded Downlink Pump

- [x] 6.1 Decode active Event, batch, drop-summary, policy, ping/pong, error, and disconnect types through the continuous decoder with negotiated capability checks and a bounded completed-frame quantum per receive callback.
- [x] 6.2 Add exact route and copied-sequence validation, receiver-local deadlines, atomic batch admission, deterministic encoded-byte accounting across combined FIFO plus charged in-flight publication, saturating drop/expiry diagnostics, separately audited decoder/ingress/subscriber bounds, and terminal no-eviction overflow.
- [x] 6.3 Add one downlink bucket, FIFO plus exact one-node-per-FIFO-item indexed min-heap, direct in-flight deadline, gate-linearized/captured-time/TTL-rechecked publication, exact token-or-earliest-TTL decision wake, bounded shared publish/expiry quantum with immediate due continuation, owner-unavailable mapping, and zero-rate expiry without polling or tombstone rebuilds.
- [x] 6.4 Add barrier-controlled deterministic fragmentation/coalescing, single/batch route and sequence matrices, atomic invalid/oversized batch, frame-work limit, combined FIFO/in-flight byte/count overflow, heap node/cardinality/removal invariants, multi-quantum zero-rate and token-later-than-TTL expiry, burst/refill/pause/resume, complete policy transactions during publication, terminal-before/after-publication, stream-subscriber isolation, hostile diagnostic, and cleanup tests without sleeps.

## 7. Boundaries, Integration, Documentation, and Archive

- [x] 7.1 Add production secure-channel integration coverage that performs admission handoff, flow-policy activation, bidirectional Events, transport backpressure, and terminal teardown without live Bonjour dependence.
- [x] 7.2 Prove the active pump claims no process lease, mutates no supported NearWire state, starts no reconnect/lifecycle/persistence/Keychain/UI/performance work, creates no channel/decoder replacement, and adds no supported SwiftPM/CocoaPods API, product, target, dependency, subspec, entitlement, or privacy declaration.
- [x] 7.3 Add English active-pump documentation covering ownership, policy direction, effective rates, queue/sequence/TTL semantics, buffering, backpressure, cancellation, security/non-delivery limits, diagnostics, and residual public-connect scope; update roadmap and README status.
- [x] 7.4 Run focused and full platform tests, strict-concurrency builds, production TLS integration, packaging, CocoaPods, API inventory, boundary, structure, English, formatting, version, validation-tool, and strict OpenSpec gates.
- [x] 7.5 Capture exact commands, run identity, counts, expected notes, API inventory, cross-actor linearization audit, queue/decoder/ingress/in-flight/subscriber retention audit, task/timer/power audit, requirement-to-evidence mapping, and residual scope under the active change evidence directory.
- [x] 7.6 Run independent post-implementation architecture/API, correctness/testing, and security/performance/documentation review rounds; record and fix every actionable finding and repeat fresh rounds until all report zero.
- [x] 7.7 Complete the spec-to-evidence audit, mark every task complete only after evidence exists, validate strictly, archive, and commit before `sdk-public-connect` apply begins.
