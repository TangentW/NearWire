# Planning Validation

- Existing SDK inspection confirmed public default-disabled bounded recovery plus explicit `suspendConnection()` and `resumeConnection()` with exact cleanup and fresh-route semantics.
- Existing Demo inspection confirmed `NearWire()` default construction and no scene-phase forwarding.
- Existing Viewer inspection and archived evidence confirmed newest-session exact-route replacement and live Event stability, while recording that the prior physical-iPhone reconnect smoke was unavailable.
- Current device discovery reported one booted iPhone 17 Pro simulator and one Wi-Fi-connected physical iPhone 17 Pro; physical execution remains subject to existing user-owned signing and XCTest availability.
- No source or test file was modified before proposal, design, capability delta, and tasks were complete.
