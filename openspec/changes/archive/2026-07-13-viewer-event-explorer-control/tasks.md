## 1. Change Gate

- [x] 1.1 Complete and strictly validate the proposal, design, new capability spec, modified local-store capability spec, modified multi-device capability spec, and task plan before modifying production or test source.
- [x] 1.2 Obtain lightweight independent architecture/API, correctness/testing, and security/performance/documentation reviews of the artifacts and resolve every actionable finding.

## 2. Explorer Store Facades

- [x] 2.1 Migrate valid schema 1 to schema 2 on one tokenized off-MainActor migration-writer executor by adding exactly the scoped Event-UUID, all-device gap-order, and device-scoped gap-order indexes in one rollback-safe transaction. Use system-default-VFS disk-backed sorting only after current-user/0700/nonsymlink process-temp verification, never mutate global SQLite/environment temp routing or add a VFS, add a migration-only 32-MiB cache target, checked `512 MiB + 6 * allocated footprint` preflight and 256-MiB floor on both database/temp volumes, 10,000-step cancellation/progress, safe phases, once-per-process automatic attempt, explicit retry, final in-transaction plans/probes, first-create version 2, content preservation, and fail-closed schema-1 rollback. Always close the migration writer and join to zero sorter descriptors; on success publish only a freshly opened/probed normal writer and readers with memory temp and explicit 8-MiB cache targets.
- [x] 2.2 Add a ViewerStoreRuntime-owned explorer gateway with immutable coordinator-generation operation leases/tokens. Seal/cancel/join/release originating work before coordinator close/reopen, reject stale work with one closed result, and never expose or retain coordinator services in the application.
- [x] 2.3 Implement one non-MainActor query arbiter as sole traversal/lease owner. Serialize page/detail/gap/causality/filtered-export-scope work; tokenize every interactive-reader operation from enqueue through completion; make queued/completed cancellation successor-safe; end traversals exactly once; and give filtered export an immutable scope plus independent export lease.
- [x] 2.4 Add frozen recording/device catalogs with immutable row-ID/connection-ordinal keysets, upper row/change/store generation fingerprints, explicit restart on relevant mutation, 50/100 defaults, 100/200 maxima, 2,000,000-step/250-ms budgets, accepted plans, closed errors, and no Event content or mutable activity ordering.
- [x] 2.5 Bind new durable device `logicalID` to exact admission connection ID; add exact aliases/journal identity and complete detail metadata; add 32-row frozen gap diagnostic traversal and nine-row/`hasMore` causality lookup with deterministic edge/cycle rules, schema-2 indexes, plan/work gates, and originating lease ownership. Preserve pre-existing closed rows without migration.
- [x] 2.6 Expose revision-safe rename/note/annotation/pin/delete and complete/filtered export preflight/execution through the runtime gateway without exposing SQLite, paths, SQL, raw errors, stale coordinator operations, or shared mutable query leases to MainActor.

## 3. Live Presentation and Downlink Composition

- [x] 3.1 Replace the untyped handoff-owner factory/downcast with one per-startRuntime components factory/bundle carrying explicit runtime ID, same manager/handoff/control facade, manager generation, live projection, composite journal, explorer inputs, and cleanup receipt; keep process store runtime outside and prove shutdown order across every stop/reset/failure path.
- [x] 3.2 Create one normalized immutable committed observation shared by store/live paths with one Viewer receive time, exact runtime/connection identity, frozen bounded session metadata, Event value/precomputed bytes, direction/sequence, and initial disposition. Define one live/store durable-projection comparator over persisted Event fields, nearest-millisecond App-created time, and initial disposition; explicitly exclude exact-session source/target/epoch invariants, session metadata, deterministic accounting, and newly sampled Viewer receive times. Linearize it at live ingress for the pending/retained horizon, make identical values idempotent and conflicts first-wins there; after eviction rely only on an existing durable row's identical/typed-conflict decision, disclose lost horizon, and retain no unbounded key tombstones.
- [x] 3.3 Implement 64-record/20-MiB constant-work live ingress plus one drain/dirty successor and an O(1) exact-key deque retaining 512 records/32 MiB. Keep large release/eviction off callback locks; add bounded session aliases, later dispositions, drop/session/store-gap state, latest-only 10-Hz wake, immutable snapshots, content-free reflection, and joined runtime clearing.
- [x] 3.4 Implement bounded live evaluation with exact metadata/JSON/presence scopes, explicit durable-only FTS behavior, 512-entry/32-MiB/16,384-predicate/1,000,000-node/100-ms limits, checkpoints, no partial-complete publication, and differential SQLite coverage where semantics are shared.
- [x] 3.5 Add manager-issued opaque runtime/generation/connection/token-UUID capabilities, at most 16 active entries, and a separate exact terminal cache capped at 64 entries/30 seconds with equality expiry, deterministic time/UUID eviction, reconnect independence, and shutdown/reset clearing. Add one encode-once immutable prepared draft; define authoritative ordered duplicate/wrong-runtime/generation/never-issued/expired/evicted/terminal/nonactive/queue classifications inside manager/session ownership, O(1) per-target checks using precomputed bytes, and no retry, retarget, delivery claim, or independent history.

## 4. Explorer Presentation Model

- [x] 4.1 Implement one MainActor explorer model with one runtime/presentation generation state machine; 200 recording, 200 device, 600 Event, 128 gap, cursor/reload-anchor, 16-selection, and one-detail resident caps; deterministic bidirectional eviction/reload; and a one-wake/10-Hz latest-only refresh cadence.
- [x] 4.2 Implement source-neutral explorer scope/filter values for current-runtime or historical source and all or 1-through-16 exact logical devices; compile only materialized durable IDs into `ViewerEventQuery`; implement all V1 durable filter dimensions, the bounded transient subset/full-text exclusion, atomic current-to-durable transition, exact AND/OR/presence semantics, one immutable live snapshot per evaluation, fixed refine guidance, and no synthetic IDs, omitted selection, replay, guessed FTS, partial completion, or fallback widening.
- [x] 4.3 Implement Pause generation invalidation before freeze, auto-follow, Jump to Latest, exact-once arbiter release, resume-by-fresh-traversal, transient/durable reconciliation, and diagnostic gap lane without pausing networking, persistence, cleanup, live admission, or downlink sending.
- [x] 4.4 Implement the immutable registry and bounded raw/tree/log/table/numeric preparation: one canonical detail buffer, 64-KiB raw chunks, 1-MiB pretty input/2-MiB output/100-ms bound, shared-range tree with 128-child chunks/4,096 nodes/preview and accessibility caps, log 1-MiB input/64-KiB output/4-KiB chunks/100-ms bounds, table 1-MiB input/4,096-scan/64-page/128-retained/512-KiB-derived/100-ms bounds, structured control/bidirectional escaping, numeric one-detail-at-a-time 200-row/8-MiB/100-ms scan, exact generations, and fixed fallback/refine states.
- [x] 4.5 Implement incrementally accounted capped text buffers and one replaceable composer-preparation generation for type, checked JSON content/model/16-MiB queue formula, `UInt64` TTL with at most nine ASCII digits, search, JSON path/value, name, note, and annotation; avoid per-keystroke full rescans and count encode/traversal/copy operations.

## 5. Native macOS UI

- [x] 5.1 Build the single-window three-column source/timeline/inspector layout with the bottom composer while preserving pairing, approval, Pause New Devices, device telemetry/rates/nickname, storage settings, and identity controls.
- [x] 5.2 Add current/historical recording and device selection, virtualized single/merged timelines, search/filter controls, pause/resume and tail controls, transient/gap/disposition presentation, JSON tree/raw/metadata/causality detail, and safe empty/error/loading states.
- [x] 5.3 Add recording rename/note/annotation/pin/delete workflows and complete/filtered export selection with revision-safe confirmation, mandatory disclosure, native save panel, progress/cancel, and no destination persistence.
- [x] 5.4 Add the multi-target control composer with type, JSON, priority, TTL, normal/keep-latest inputs and exact `Queued locally` result wording; keep templates, favorites, independent send history, reserved platform type entry, and delivery claims absent.
- [x] 5.5 Add keyboard navigation, focus order, combined accessibility labels/hints, non-color state, scalable layout, deterministic English presentation, standard clipboard editing only in operator-owned inputs, and no received/stored Event inspector copy/cut/drag/share/clipboard-export command, background pasteboard access, or content leakage into safe device/status rows, logs, reflection, preferences, recent rows, or restoration. Preserve the separately disclosed JSON file-export workflow.

## 6. Tests, Documentation, and Evidence

- [x] 6.1 Add deterministic schema-1-to-2 migration/rollback/index/plan tests with large populated fixtures, unsafe/symlink/wrong-mode process-temp rejection, distinct database/temp near-capacity and overflow preflight, proof that global temp routing/VFS is unchanged, each-index and final-probe failure, exact cancellation/termination and retry, once-per-process behavior, WAL/temp/heap high-water, system sorter-file mode/no-content and zero-descriptor cleanup, 250-ms injected cancellation acknowledgement, <=128-MiB heap growth, post-rollback schema-1 and post-success schema-2 probes, and proof that the migration connection closes before a fresh normal writer/readers publish with memory temp and 8-MiB caches; add coordinator-generation/query-arbiter races for page/detail/catalog/gap/causality/source replacement, token cancellation after successor start, lease refresh/expiry/exact release, filtered-export scope, and close/retry/reopen.
- [x] 6.2 Add frozen catalog/timeline/gap/causality tests for equal row/time bounds, commits/rename/pin/tombstone between pages, explicit restart, forward/backward long scroll, resident eviction/reload, current-to-durable transition, single/all/selected devices, gap revisions/overlap, and 0/1/2/8/9+ UUID candidates plus real/false cycles.
- [x] 6.3 Add shared-observation/live tests for no durable parent, partial device materialization, one/2-through-16/all logical selections, atomic current-to-durable transition and filter replacement, exact receive-time boundaries, every metadata/JSON/presence predicate, durable/live differential results, FTS transient exclusion, disposition/drop/session/store-gap transitions, and duplicate equality/conflict across pending/drain/eviction/`untracked`/durable/recovery/shutdown. Require metadata/accounting/receive-time-only and sub-millisecond-created-time differences that normalize to the same millisecond to remain equal and preserve the first values; require any persisted-projection/initial-disposition or normalized-millisecond difference to conflict; prove source/target/session-epoch mismatch is rejected before journal commit and no hash alone decides. Cover 64/20-MiB ingress and 512/32-MiB window bounds, maximum shapes, unavailable/recovered storage, reconnect sequence reuse, exact store status/rows/live markers, and callback/drain counts.
- [x] 6.4 Add control tests for exact content/model/16-MiB queue formula boundaries, a model limit larger than content limit, standard operator-input paste with multibyte replacement and over-cap rejection before model storage, TTL empty/sign/space/overflow/range boundaries, capped rapid edits, one encode/traversal/copy, opaque target capabilities, all-duplicate/wrong-runtime/generation/never-issued/expired/negotiating/disconnecting/queue/terminal cases, same-route reconnect, 64/65 terminal entries, equal disconnect times, exact 30-second boundary, capacity eviction, reset/shutdown clearing and no retargeting, blocked/mixed 16-target ordered results, superseding attempts, shutdown admission gate, bounded MainActor storage/copies, and truthful local wording/reflection/preferences/clipboard behavior. Exercise actual macOS text controls plus keyboard/contextual copy/cut/paste: operator inputs allow bounded editing, received/stored inspector controls expose no copy/cut/drag/share/clipboard-export command, the separately disclosed JSON file-export workflow remains available, and no background pasteboard read occurs.
- [x] 6.5 Add presentation/renderer tests for Pause-before-completion, rapid Pause/Resume/Jump/filter generations, 100,000 change-token bursts, exact wake/query counts, full-window long scrolling, one selected detail, Generic fallback, depth-128/100,000-entry/1-MiB key/message/maximum Event, mixed scalar and control/bidirectional content, independent log/table byte/row/time/preview/VoiceOver caps, chunk/tree/accessibility caps, 200-row numeric scan, cancellation, rapid selection, safe reflection, retained-value/derived-byte counts, and stale-update absence.
- [x] 6.6 Add history-operation/export integrations for metadata revisions, pin/cleanup protection, stale delete confirmation, active/leased rejection, transient exclusion disclosure, complete/filtered export, cancellation/atomic destination, coordinator replacement, and UI-safe failures.
- [x] 6.7 Add blocked-operation cleanup tests for catalog/query/gap/causality/detail/live-match/raw/tree/renderer/export/composer at close, runtime replacement, listener failure, TLS/full reset, and termination; prove finite combined receipt, no late enqueue/presentation/accessibility, zero content tasks/canonical-detail/derived-renderer/filter/composer/validation/accessibility buffers or subscriptions, redacted reflection including cancelled values, exact lease release, and no prior content after a fresh runtime starts.
- [x] 6.8 Add English Event Explorer/operator documentation covering receive-time semantics, transient versus recorded rows, source/device materialization, duplicate horizon, schema-update status/disk/retry behavior, deterministic-versus-heap accounting and diagnostic-only timing, resident/refresh/work bounds, FTS outage exclusion, gap lane, pause semantics, renderer/refine and structured-control escaping, composer content/TTL bounds, causality ambiguity, local queue admission, export disclosure, privacy, cleanup, and exclusions.
- [x] 6.9 Build and test the Viewer scheme and all affected package suites. Gate every normative count/byte/logical-deadline/generation/token/VM/page/cursor/wake/lease limit; use the 100,000-Event/10,000-gap migration fixture for <=128-MiB heap growth and <=250-ms injected cancellation; use 100,000 live offers for exact ownership/operation/wake counts; save query plans/VM work/transactions, renderer/live/composer counters, retained rows/pages/cursors/content buffers, format/accessibility/privacy/project/root-package inspection, and strict OpenSpec results. Record callback wall time and whole-process heap only as diagnostic machine context paired with structural gates, never as product guarantees. Do not add a shell harness.

## 7. Independent Completion Review

- [x] 7.1 Obtain independent architecture/API, correctness/testing, and security/performance/documentation implementation reviews and save each report.
- [x] 7.2 Fix every actionable finding, rerun affected and complete validation, and repeat all three review dimensions until a fresh round reports zero unresolved findings.
- [x] 7.3 Complete a requirement-to-evidence audit, archive `viewer-event-explorer-control`, verify canonical specs and archived evidence, and commit the completed change before starting `viewer-performance-dashboard`.
