# Requirement-to-Evidence Audit

Date: 2026-07-13 (Asia/Shanghai)

| Requirement area | Implementation evidence | Executable or inspection evidence | Result |
| --- | --- | --- | --- |
| One hardened local SQLite store and explicit schema/failure boundaries | `ViewerSQLite`, `ViewerStoreSchema`, three serialized connection roles, guarded Application Support paths, schema probes, generation-bound cancellation | Store creation/reopen/unknown and incomplete schema, checked binding/rollback, symlink/permission, progress-budget, sustained-WAL, system-linkage, privacy-resource, and filesystem audits; Round 16 full Store/Viewer gates | Satisfied |
| Automatic runtime and device lifecycle recording | `ViewerStoreRuntime`, `ViewerStoreCoordinator`, stable runtime/device contexts, bounded recovery claims, causal materialization, orphan reconciliation | Multi-device lifecycle, unavailable startup/retry, failed recovery claim, ended-during-outage, orphan, same-ID, sequential reopen, late cleanup, superseded construction, final-current shutdown, and rapid application-stop tests | Satisfied |
| Bidirectional Event outcomes are journaled without protocol authority | `ViewerEventStore`, immutable Event commits, append-only dispositions/transitions, downlink admission observations, protocol-independent store sink | Idempotent/conflicting disposition tests, duplicate peer-ID ownership, downlink mailbox retry/commit, expiry/displacement/session-end, transition-loss gap, and storage-failure independence tests | Satisfied |
| Store ingress and write transactions are finite and nonblocking | Bounded Event ingress, fixed structural lane, one drain plus one dirty successor, normal and one-record oversize transactions, coalesced gaps | Shared-budget, failed-prefix retry, writer failure ordering, maximum legal Event, full-ingress close, impossible observation, saturation, reflection, and package queue/transport regressions | Satisfied |
| Storage preferences use bounded 3 GiB and seven-day defaults | `ViewerStoragePreferences`, versioned bounded persistence, corruption recovery, revision-bound application | Default/corrupt/out-of-range preference, safe persistence, settings revision supersession, automatic recovery revocation, and application settings tests | Satisfied |
| Cleanup is transactional, whole-session, and protection aware | `ViewerStoreMaintenance`, bounded campaigns, transactional tombstones, retention-first selection, quota/capacity/volume guards, bounded reclaim/checkpoint work | Exact 85%/100% capacity, protected/pinned/leased data, projected reservations, rollback, huge recording, oversize reclaim, checkpoint/vacuum, disk-floor, periodic-wake cancellation, and failure-recovery tests | Satisfied |
| Pin, metadata, annotation, and manual deletion are revision safe | Bounded metadata validators, append-only annotations, pin state, revision-bound single-use confirmations, shared bounded reclaimer | Text-bound/control tests, stale and single-use confirmation, annotation invalidation, active/leased rejection, pinned deletion, and writer-ordering tests | Satisfied |
| Full-text and JSON queries are validated and indexed | `ViewerStoreQuery` normalized models, closed JSON grammar, bound parameters, literal FTS, binary prefix and `instr` semantics, plan gates | Query truth-table and combined-dimension tests; wildcard/operator/comment/quote/control/Unicode literalization; impossible type/non-ASCII index rejection; `EXPLAIN` plan checks | Satisfied |
| Event results use stable bounded keyset pagination | Frozen append-only bounds, recording leases, query fingerprints, forward/backward tuple cursors, finite per-page budgets | Bidirectional frozen traversal, equal-time tie breaking, later Event/transition exclusion, cursor mismatch, cleanup/lease/expiry, point detail, and work-limit tests | Satisfied |
| JSON export streams a frozen complete or filtered result | `ViewerStoreExport`, one finite lease, frozen row-ID bounds, stored aliases, paged streaming, descriptor-relative atomic commit | Large/frozen filtered export, alias/forbidden-field disclosure, lease expiry, cancellation/failure commit boundary, hard-link/parent substitution, sustained-write/cleanup race, bounded page/buffer tests | Satisfied |
| Storage status and settings expose no Event data | Latest-only safe status/change signals, bounded refresh seam, native storage settings in `ViewerRootView`, closed descriptions/reflection | Application storage settings/status tests, populated refresh reflection, sensitive model/root reflection, accessibility/presentation inspections, English Viewer local-store guide | Satisfied |
| Shutdown performs one finite owned flush | Runtime/store cleanup receipts, maintenance quiescence, reserved structural close, one terminal flush, exact construction leases | Maintenance-before-flush, failed flush/capacity reconciliation, preexisting failed-prefix, orphan next-open, matching/nonmatching/final-current construction waits, terminal close, and rapid application-stop tests | Satisfied |
| Modified drop telemetry remains bounded and content-free | Saturating policy/drop samples, latest-only safe signals, closed telemetry carriers, store-only persistence boundary | Drop journal monotonicity/coalescing, remote sample, safe reflection, recent-row bound, status failure, built privacy manifest, and complete flow-control/package regressions | Satisfied |
| Multi-device owner exposes bounded journal observations without transferring protocol ownership | Internal immutable journal values and nonthrowing bounded sinks in `ViewerMultiDeviceSession` and manager; storage admission in Viewer only | Blocked/unavailable sink, protocol-control progress, queue/mailbox retry, deterministic byte-bound, storage-pressure, terminal cleanup, and store integration tests | Satisfied |
| Packaging, compatibility, privacy, and repository boundaries | Root-only package/podspec, Swift 5 mode, iOS 16/macOS 13, system SQLite in Viewer, no Core/SDK runtime dependency, unchanged privacy resource | Round 16 manifest dump, hashes, nested-manifest scan, podspec syntax, SwiftPM 536-test gate, Viewer 173-test gate, SQLite linkage, privacy `cmp`, format/hygiene/OpenSpec checks | Satisfied |
| Configured signing and entitlement boundary | No signing model changed by this Viewer-local-store change | Unsigned behavior is verified; the stable-signer probe and final signed entitlement assertion are explicitly deferred by product-owner decision to goal-level `release-hardening` | Deferred, not represented as passing |

All non-signing requirements and scenarios have direct implementation plus executable or inspection
evidence. `implementation-validation-round16.md` is the authoritative final-tree validation record.
Round 16 independent architecture/API, correctness/testing, and
security/performance/documentation reviews each report exactly zero unresolved actionable
findings. No implementation finding is accepted by this audit. The only deferred items are the
two configuration-dependent signing checks listed above, which remain part of the overall Goal's
final `release-hardening` gate.
