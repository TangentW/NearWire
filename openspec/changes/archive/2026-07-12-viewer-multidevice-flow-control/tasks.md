## 1. Change Gate

- [x] 1.1 Complete and strictly validate the proposal, design, capability spec, and task plan before modifying production or test source.
- [x] 1.2 Obtain lightweight independent architecture/API, correctness/testing, and security/performance/documentation reviews of the artifacts and resolve every actionable finding.

## 2. Session Ownership and Protocol Continuation

- [x] 2.1 Extend the internal Core frame decoder with bounded paused-on-complete-frame/needs-more-bytes/drained progress and retained-byte observation; add one idempotent generation-bound `SecureByteChannel` receive-pause token that can be claimed during synchronous delivery, prevents rearm, resumes once, and invalidates terminally; then extend `ViewerAdmissionConnectionCore` with synchronous reentrant one-time same-core session attachment, sole protocol executor, coalesced-frame preservation, total input accounting, one token/ordered-suffix continuation, detach-before-resume partial-frame handling, channel admission, terminal cleanup, explicit lock ordering, and rollback without exposing or replacing Network.framework values, callback ownership, decoder ownership, or the terminal gate.
- [x] 2.2 Implement the 16-slot `ViewerMultiDeviceSessionManager`, provisional-slot rollback, per-session isolation, unauthenticated logical correlation, live-duplicate rejection, connection-bound downlink ownership, exact handle cleanup, at most 64 deterministic 30-second recent rows, one replaceable bounded expiry wake, and latest-only main-model snapshots.
- [x] 2.3 Implement Viewer hello acknowledgement, fresh session epoch, injected non-resetting 10-second initial/dynamic policy deadlines, conservative acceptance validation, active codec transitions, one in-flight V1 offer, latest requested-policy coalescing, exact acceptance/timeout/terminal winner rules, and per-session terminal categories.

## 3. Preferences, Queues, and Bidirectional Pumping

- [x] 3.1 Implement injected, versioned, bounded `UserDefaults` storage for the 20/10 global defaults, 256 Bundle-ID policies, 256 logical-route nicknames, deterministic LRU eviction, safe corruption recovery, and session-override precedence without persisting effective policy.
- [x] 3.2 Implement per-session 5,000-Event/16-MiB uplink-delivery and downlink-send queues, negotiated single-Event bounds, normal and keep-latest downlink APIs, exact route/epoch/sequence/codec/TTL validation, atomic inbound/downlink sequence commit, nonblocking uplink sink delivery, connection-bound ownership, and memory-only terminal clearing.
- [x] 3.3 Implement independent directional and ingress-contract token buckets, 500 ms downlink batching, reserved Control mailbox capacity, bounded callback/record/system/service quanta, coalesced one-shot scheduling, zero-rate enforcement, local coalesced drop summaries, remote-summary telemetry, and saturating safe snapshots without recurring idle timers.

## 4. Application Model and Workspace

- [x] 4.1 Wire the live multi-device owner into the existing listener runtime and cleanup receipt while preserving foundation admission, pause, refresh, reset, collision, and window-lifecycle behavior.
- [x] 4.2 Replace the workspace placeholder with the connected-device sidebar and selected-device detail for state, nickname, requested/effective rates, queues, throughput, Event counts, drops, validation, accessibility, stable selection, and per-device disconnect.
- [x] 4.3 Keep Event content, timeline/search/filter/store/export controls, control composition, and performance charts absent while exposing internal seams for later Viewer changes.

## 5. Tests, Documentation, and Evidence

- [x] 5.1 Add deterministic unit tests with injected monotonic time and barriers for preference precedence/validation/eviction/corruption; exact-tuple duplicate rejection in both admission policies; same-installation missing/different-Bundle variants as separate non-inheriting rows; the exact 16-slot boundary for pure and mixed provisional/negotiating/active/disconnecting ownership through cleanup; 64-row multi-wave churn, deterministic eviction, exact 30-second boundary, one expiry owner, and zero shutdown ownership; initial/dynamic/lower/zero/escalation/no-pending-repeat/indistinguishable-lower-attribution policy behavior; timeout before and after continuation for an acceptance beyond the first quantum at deadline-minus-one, equality, and deadline-plus-one; physical-terminal/shutdown suffix winners; exact requested/effective/deadline-elapsed state and close count; and same-core coalesced-frame/double-attach/rollback/terminal/shutdown races.
- [x] 5.2 Add controllable-driver Core channel/decoder and Viewer queue/pump tests for bounded retained suffix pause/resume; paused/needs-more/drained progress; no receive rearm or second callback while a complete frame is paused; exactly one generation-bound token/continuation; ordinary partial-tail detach-before-resume with immediate driver completion and a fresh token; recorded-timeout partial/drained no-resume cleanup; resume-versus-terminal in both orders; total decoder/callback byte accounting; stale resume and terminal zero-residue behavior; split-versus-coalesced equivalence under equal injected receipt samples; partial-frame completion under a later sample; continuation-delay invariance across TTL/token/policy-deadline/throughput decisions; count/byte/single-Event bounds; receiver-local TTL plus sequence commit; priority overflow plus sequence commit; normal/keep-latest pre-admission behavior; route validation with zero commit; atomic mailbox rejection/retry; earlier accepted frames plus later rejection; exact connection-bound terminal drops; maximum legal frame and 256-record batch atomicity; 500 ms batching; Control reservation; ingress/system/token storms; 33-through-128 system bursts; drop-summary coalescing; terminal/timeout-versus-suffix races; telemetry saturation; exact queue/token/fairness/sequence state; and shutdown clearing.
- [x] 5.3 Add integration tests for 1, 4, 8, and 16 concurrent sessions, a barrier-controlled 16-owner mixed provisional/negotiating/active/disconnecting registry with 17th rejection, duplicate live-route preservation, repeated distinct-route churn, more-than-64 valid small-frame coalescing, slow/full/invalid/over-rate/drop-summary-storm device isolation, 16-session contention with progress between continuation turns, real SDK-compatible handshake and bidirectional Event exchange, pause/refresh preservation, and exact multi-session handle/slot/task cleanup.
- [x] 5.4 Add presentation/SwiftUI tests, closed diagnostic/description/reflection tests, and English architecture/operator documentation covering unauthenticated identity hints, rates, work/queue bounds, sequence semantics, telemetry, failure categories, memory-only behavior, privacy-manifest rationale, exclusions, and the next Viewer change.
- [x] 5.5 Build and test the Viewer scheme plus all affected Core/SDK suites; inspect the built privacy manifest and record whether existing Device ID/UserDefaults declarations cover the new bounded preferences; run the repository bootstrap gate, strict OpenSpec validation, diff/format/language/structure/package checks, and save exact commands and results under this change's `evidence` directory. Do not add a new shell harness.

## 6. Independent Completion Review

- [x] 6.1 Obtain independent architecture/API, correctness/testing, and security/performance/documentation implementation reviews and save each report.
- [x] 6.2 Fix every actionable finding, rerun affected validation, and repeat all three review dimensions until a fresh round reports zero unresolved findings.
- [x] 6.3 Complete the requirement-to-evidence audit, archive `viewer-multidevice-flow-control`, verify canonical specs and archived evidence, and commit the completed change before starting `viewer-local-store-search`.
