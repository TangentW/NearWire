## 1. Planning and Pre-Apply Gate

- [x] 1.1 Complete and strictly validate the proposal, design, new public-connect capability, all modified capability deltas, and this task plan.
- [x] 1.2 Run independent architecture/API, correctness/testing, and security/performance/documentation reviews of the planning artifacts; record every finding, remediate, and repeat fresh rounds until all three dimensions report zero unresolved findings.

## 2. Public Models, Limits, Identity, and Errors

- [x] 2.1 Add supported connect error codes, the normative preflight precedence, and exhaustive content-safe mapping for every error observable before connect succeeds; classify post-return terminal reasons only as disconnected.
- [x] 2.2 Add fixed SDK version and actual-String-only `Bundle.main` metadata construction, with validation proving the compiled value matches root `VERSION`, documented fallback order, and invalid optional-field omission.
- [x] 2.3 Add a constant-space overflow-safe least-privilege planner with a fixed deterministic-content plus exact non-content record formula, reviewed-default minima, two Control reservations, active-turn accounting, symmetric decoder/incoming bounds, and no buffer-to-network-limit coupling.
- [x] 2.4 Prove the formula with structural analysis plus adversarial/property production encodings for every JSON, escaping, Unicode, structural, numeric, identifier, causality, timestamp, sequence, TTL, and schema case; add exact/one-over downstream tests, hostile maximum Event/batch/overflow coverage, and peak-retention evidence.
- [x] 2.5 Add the bounded off-actor installation identity worker with permanent literals, exact read/add dictionaries, data-protection Keychain, `kSecUseAuthenticationUISkip`, `WhenUnlockedThisDeviceOnly`, 36-byte canonical V4 UUID validation, `SecRandomCopyBytes`, one duplicate reread, injected production-equivalent operations, and no deprecated constant or overwrite/delete surface.
- [x] 2.6 Add table-driven identity transcripts for hit; miss/add success; protected skip/add-duplicate/skip failure; initial read failure; randomness failure; ordinary add failure; duplicate/reread valid; and duplicate/reread missing, malformed, noncanonical, unexpected type, or access failure, asserting exact attributes and read/random/add/update/delete counts plus no access before lease.

## 3. Tokenized Public Connection Orchestration

- [x] 3.1 Add immutable internal public-connect dependencies and named barriers around lease, identity, admission target/result, phase authorization, activation target/result, terminal-wait claim/registration, transfer, actor commit, terminal delivery, and release, without allowing tests to bypass production validation.
- [x] 3.2 Add one exact-token actor slot and one transition gate created before connect suspension and passed by identity through admission and lifetime, with cancellation/terminal chronology, generation-tagged targets, same-gate lease handoff, stale completion disposal, and retained-resource snapshots.
- [x] 3.3 Implement connect in strict order: preflight and limit plan, slot reservation, lease claim, off-actor identity, hello construction, immediate public pairing-reference release after admission construction, discovering, admission, activation, transfer claim, actor owner commit, connected, and return.
- [x] 3.4 Add shared synchronous attempt authorization plus a connecting observer that returns a closed result, with state/Task/gate checks before and after suspension and zero core/channel work after shutdown even when actor cancellation delivery is held.
- [x] 3.5 Add one session lifetime whose gate is synchronously marked by the core at exact terminal transition, plus one coordinator created after atomic lease/cancellation handoff that owns exactly one wait and release through attachment, activation, connected state, shutdown/deinit, and terminal.
- [x] 3.6 Refactor active binding to capture rates by value, remove strong core owner storage, make all owner operations weak and owner-unavailable-aware, and preserve gate/wake cleanup without a core-to-NearWire retain path.
- [x] 3.7 Add critical-section both-winner tests for cancellation/target replacement and core terminal versus transfer/connected claims; add deterministic public async barriers for admitted result, activation result, phase authorization, shutdown, and release-before-delivery; audit exact-token stale callbacks plus the single coordinator wait/release; and prove supported retry outcomes without sleeps.
- [x] 3.8 Add retention tests proving a live connect Task retains NearWire, while post-success external release deinitializes NearWire, drops the hidden handle, terminates the core, and releases through the sole coordinator; prove no coordinator, Task, core, live operation, channel, or callback retains NearWire or pairing data.
- [x] 3.9 Add injected lease-runtime tests for claim-exit, release-enter, release-exit, repeated and stale release, and retry attempts, proving fail-closed behavior without unsupported reacquisition claims.
- [x] 3.10 Add every pre-admission release branch for ordinary failure, Task cancellation, and shutdown during identity, discovery, phase, and admission; separately assert attached-slot release-before-clear versus immediate shutdown detachment/no-later-mutation, pending result after release, no coordinator/wait, and retry only after successful synchronization.

## 4. Boundaries, Integration, Documentation, and Archive

- [x] 4.1 Update SwiftPM and CocoaPods public consumer fixtures for connect, state, and errors; prove Security linking parity and that no internal orchestration, Keychain, Network, Security, lease, admission, or pump type is exposed.
- [x] 4.2 Add production secure-channel integration through supported connect with deterministic discovery, proving activation, bidirectional Events, terminal cleanup, contention until terminal, and lease reacquisition only after terminal plus successful synchronization.
- [x] 4.3 Update English public API, connection, security, README, and roadmap documentation with all automatic hello fields, device-only Keychain persistence/correlation and no reset, public pairing-code semantics, unauthenticated Viewer threat model, state/error behavior, no delivery guarantee, and residual lifecycle scope; run a terminology audit against authentication overclaims.
- [x] 4.4 Run focused and full platform tests, strict-concurrency builds, production TLS integration, packaging, CocoaPods, API inventory, boundary, structure, English, formatting, version, validation-tool, and strict OpenSpec gates.
- [x] 4.5 Capture exact commands, run identity, counts, API inventory, preflight/error-map audit, constant-space limit and peak-retention proof, terminal-winner/one-wait audit, state/ownership linearization audit, Keychain transcript/security audit, retain-graph/task/timer audit, requirement-to-evidence mapping, and residual scope under evidence.
- [x] 4.6 Run independent post-implementation architecture/API, correctness/testing, and security/performance/documentation review rounds; record and fix every actionable finding and repeat fresh rounds until all report zero.
- [x] 4.7 Complete the spec-to-evidence audit, mark tasks complete only after evidence exists, validate strictly, archive, and commit before `sdk-connection-lifecycle` apply begins.
